<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- DPAPI (System.Security.Cryptography.ProtectedData) is Windows-only; the whole
         app targets Windows only (SAD §4.1), so this is not a portability concern. -->
    <TargetFramework>net10.0-windows</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Konscious.Security.Cryptography.Argon2" />
    <PackageReference Include="System.Security.Cryptography.ProtectedData" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Darhous.Archive.Core\Darhous.Archive.Core.csproj" />
  </ItemGroup>

</Project>
